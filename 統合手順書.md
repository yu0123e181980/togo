# データ分析ツール統合手順書

## 概要
7つの独立したHTMLツールを1つの統合HTMLファイル（all_tools_integrated.html）に統合する手順を説明します。

## 前提条件
- 統合テンプレート（all_tools_integrated.html）が作成済み
- 既存の7つのHTMLファイルが利用可能

## ツール一覧とファイルマッピング

| ツール名 | 既存ファイル | 読み込みファイル | タブID |
|---------|------------|--------------|--------|
| 棚割データ処理 | ⓪棚割データ処理ツール.html | shelf_data.xlsx | tabContent1 |
| 収益分析 | ①非定型月次データ可視化ツール.html | profit_analysis.xlsx | tabContent2 |
| 売上分解ツリー | ②売上分解ツール 分解ツリーver.html | sales_tree.xlsx | tabContent3 |
| 売上分解表 | ②売上分解ツール 表形式ver.html | sales_breakdown.xlsx | tabContent4 |
| 顧客分析 | ③顧客属性詳細分析ツール.html | customer_analysis.xlsx | tabContent5 |
| ビジュアルベンチマーク | ④Visual_Benchmarking_tool.html | visual_benchmark.xlsx | tabContent6 |
| ABC分析 | ⑥ABC分析ツール.html | abc_analysis.xlsx | tabContent7 |

## 統合手順（Phase 2）

### ステップ1: CSSの抽出と統合

各ツールのHTMLファイルから`<style>`タグ内のCSSを抽出し、統合ファイルに追加します。

#### 1.1 CSS競合の回避
各ツールのCSSにプレフィックスを追加してスコープを分離します。

**例：棚割データ処理ツール**
```css
/* 元のCSS */
.upload-area { ... }

/* プレフィックス追加後 */
#tabContent1 .upload-area { ... }
```

#### 1.2 共通CSSの識別
以下のような共通スタイルは統合テンプレートに既に含まれています：
- `*` (リセットCSS)
- `body`
- `.btn`, `.btn-primary`, `.btn-secondary`
- `.loading-spinner`
- `.error-message`

これらは各ツールから削除し、統合テンプレートの共通CSSを使用します。

#### 1.3 統合方法
```html
<!-- all_tools_integrated.html の<style>タグ内に追加 -->
<style>
    /* 既存の共通CSS */
    ...

    /* ========== 棚割データ処理ツール専用CSS ========== */
    #tabContent1 .upload-area { ... }
    #tabContent1 .step-indicator { ... }
    ...

    /* ========== 収益分析ツール専用CSS ========== */
    #tabContent2 .filters { ... }
    ...
</style>
```

### ステップ2: HTMLの抽出と統合

各ツールのメインコンテンツ部分を対応するタブコンテンツに配置します。

#### 2.1 抽出範囲の特定
各ツールのHTMLから以下の範囲を抽出：
- コンテナ内のコンテンツ（通常は `<div class="container">` の中身）
- ファイルアップロード部分は削除（File System Access APIに置き換え）
- ライブラリ承認モーダルは削除（統合版で一元管理）

#### 2.2 配置方法
```html
<!-- all_tools_integrated.html -->
<div id="tabContent1" class="tool-section hidden">
    <h1>棚割データ処理ツール</h1>

    <!-- ツール起動ボタン -->
    <button class="btn btn-primary" onclick="initShelfDataTool()">
        ツールを起動
    </button>

    <!-- ツールのコンテンツをここに配置 -->
    <div id="shelfDataContent">
        <!-- 既存HTMLから抽出したコンテンツ -->
    </div>
</div>
```

### ステップ3: JavaScriptの抽出と統合

各ツールのJavaScriptを統合し、スコープを分離します。

#### 3.1 スコープ分離の方法
各ツールのJavaScriptを即時関数（IIFE）でラップします。

```javascript
// ========== 棚割データ処理ツール ==========
(function() {
    'use strict';

    // ツール専用の変数
    let shelfDataWorkbook = null;
    let shelfDataParsedData = null;

    // ツール専用の関数
    function processShelfData() {
        // 実装
    }

    // グローバルに公開する関数
    window.initShelfDataTool = async function() {
        try {
            const workbook = await window.readExcelFile('shelfData');
            shelfDataWorkbook = workbook;
            // ツールの初期化処理
            renderShelfDataUI();
        } catch (error) {
            showError('データの読み込みに失敗しました: ' + error.message, 'shelfDataContent');
        }
    };

    function renderShelfDataUI() {
        // UIの描画
    }
})();
```

#### 3.2 共通関数の活用
統合テンプレートに既に用意されている共通関数を使用：
- `readExcelFile(fileKey)` - Excelファイル読み込み
- `showError(message, containerId)` - エラー表示
- `showLoading(containerId)` - ローディング表示
- `switchTab(index)` - タブ切り替え

#### 3.3 ファイル読み込み部分の置き換え
```javascript
// 元のコード（削除）
fileInput.addEventListener('change', (e) => {
    const file = e.target.files[0];
    if (file) {
        loadExcelFile(file);
    }
});

// 新しいコード（File System Access API使用）
window.initShelfDataTool = async function() {
    try {
        showLoading('shelfDataContent');
        const workbook = await readExcelFile('shelfData');
        // 以降の処理
    } catch (error) {
        showError(error.message, 'shelfDataContent');
    }
};
```

### ステップ4: イベントリスナーの調整

#### 4.1 DOMContentLoaded の置き換え
```javascript
// 元のコード（削除）
document.addEventListener('DOMContentLoaded', function() {
    // 初期化処理
});

// 新しいコード
window.initShelfDataTool = async function() {
    // タブが開かれた時に実行される初期化処理

    // イベントリスナーの設定（初回のみ）
    if (!window.shelfDataInitialized) {
        setupEventListeners();
        window.shelfDataInitialized = true;
    }
};

function setupEventListeners() {
    // イベントリスナーの設定
}
```

#### 4.2 要素IDの衝突回避
必要に応じて要素IDにプレフィックスを追加：
```html
<!-- 元のHTML -->
<button id="processBtn">処理実行</button>

<!-- 変更後 -->
<button id="shelfDataProcessBtn">処理実行</button>
```

```javascript
// JavaScript内の参照も更新
document.getElementById('shelfDataProcessBtn')
```

## ツール別統合ポイント

### 棚割データ処理ツール (⓪)
- **特徴**: フォルダ選択UI、Shift_JISエンコーディング、ZIP出力
- **注意点**:
  - 既存のフォルダ選択UIは削除（統合版のFile System Access APIを使用）
  - エンコーディング処理は保持
  - ステップインジケーターのスタイルは`#tabContent1`スコープ内に配置

### 収益分析ツール (①)
- **特徴**: 複雑なフィルタリングロジック、グラフ描画
- **注意点**:
  - Chart.jsの使用を確認
  - フィルタリング状態の管理
  - グラフ描画時のcanvas要素の参照

### 売上分解ツリー (②-1)
- **特徴**: SVGツリー描画、階層データ処理
- **注意点**:
  - SVG要素の動的生成
  - 階層フィルタリングロジックの保持

### 売上分解表 (②-2)
- **特徴**: SVGベースの表形式表示、html2canvasによる画像出力
- **注意点**:
  - 複雑なSVG描画ロジック
  - html2canvasの使用

### 顧客分析ツール (③)
- **特徴**: スプラッシュスクリーン、多段階フィルタリング、ドリルダウン
- **注意点**:
  - スプラッシュスクリーンは削除（統合版で一元管理）
  - パンくずリストの状態管理
  - テーブルソート機能

### ビジュアルベンチマークツール (④)
- **特徴**: Chart.js複数グラフ、スプラッシュスクリーン
- **注意点**:
  - 複数のChart.jsインスタンス管理
  - グラフの更新処理

### ABC分析ツール (⑥)
- **特徴**: スプラッシュスクリーン、動的テーブル、カラム並び替え
- **注意点**:
  - ドラッグ&ドロップ機能
  - 動的なカラム表示/非表示

## 検証チェックリスト

### 機能確認
- [ ] スプラッシュスクリーンが2.5秒後に非表示になる
- [ ] ライブラリ承認モーダルが表示される
- [ ] フォルダ選択ボタンが動作する
- [ ] File System Access APIでフォルダ選択ができる
- [ ] 7つのExcelファイルが正しく検出される
- [ ] タブ切り替えがスムーズに動作する
- [ ] 各ツールのタブで初期化関数が実行される

### 棚割データ処理ツール
- [ ] shelf_data.xls自動読み込み
- [ ] データ処理の実行
- [ ] ZIP出力機能
- [ ] エラーハンドリング

### 収益分析ツール
- [ ] profit_analysis.xlsx自動読み込み
- [ ] フィルタリング機能
- [ ] グラフ描画
- [ ] データ更新

### 売上分解ツリー
- [ ] sales_tree.xlsx自動読み込み
- [ ] SVGツリー描画
- [ ] 階層フィルタリング
- [ ] 購入率表示切替

### 売上分解表
- [ ] sales_breakdown.xlsx自動読み込み
- [ ] SVG表描画
- [ ] 画像ダウンロード
- [ ] デュアル表示モード

### 顧客分析ツール
- [ ] customer_analysis.xlsx自動読み込み
- [ ] 階層スライサー機能
- [ ] ドリルダウン機能
- [ ] テーブルソート

### ビジュアルベンチマークツール
- [ ] visual_benchmark.xlsx自動読み込み
- [ ] 複数グラフ描画
- [ ] フィルタリング
- [ ] グラフ切替

### ABC分析ツール
- [ ] abc_analysis.xlsx自動読み込み
- [ ] ABC判定処理
- [ ] カラム並び替え
- [ ] Excel出力

### UI/UX確認
- [ ] 日本語フォントが正しく表示される
- [ ] レスポンシブデザインが機能する
- [ ] タブ切り替え時のアニメーションがスムーズ
- [ ] エラーメッセージが適切に表示される
- [ ] ローディング表示が適切に動作する

### ブラウザ互換性
- [ ] Chrome で動作確認
- [ ] Edge で動作確認
- [ ] 非対応ブラウザで警告が表示される

### パフォーマンス
- [ ] タブ切り替えが1秒以内
- [ ] Excelファイル読み込みが妥当な時間内
- [ ] メモリリークがない
- [ ] 大容量データでもフリーズしない

## トラブルシューティング

### CSS競合が発生した場合
1. ブラウザの開発者ツールでスタイルを確認
2. より具体的なセレクタに変更（例：`#tabContent1 > .container`）
3. `!important`は最終手段として使用

### JavaScript変数の衝突
1. IIFE内で変数を宣言
2. グローバルスコープへの公開は最小限に
3. `window.ツール名_変数名`の形式を使用

### ファイル読み込みエラー
1. フォルダ内にファイルが存在するか確認
2. ファイル名が正確か確認（大文字小文字区別）
3. ブラウザのコンソールでエラーメッセージを確認

### Chart.jsの競合
1. 各グラフにユニークなcanvas IDを使用
2. 古いグラフインスタンスを破棄してから新規作成
3. `chart.destroy()`を適切に呼び出す

## 完了条件

✅ 全てのチェックリスト項目が完了
✅ 7つのツール全てが正常に動作
✅ エラーハンドリングが適切に機能
✅ ブラウザコンソールにエラーが出ない
✅ ユーザーがスムーズに操作できる

## 次のステップ

統合完了後：
1. ユーザーテストの実施
2. フィードバックの収集
3. 必要に応じて微調整
4. 本番環境へのデプロイ
